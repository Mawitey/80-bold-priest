version: 1
frontend:
  phases:
    preBuild:
      commands:
        - env | grep -e COGNITO_ >> .env.production
        - npm ci
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: .next
    files:
      - '**/*'
  cache:
    paths:
      - .next/cache/**/*
      - node_modules/**/*
